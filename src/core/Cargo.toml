[package]
name = "postframe-core"
description = "Exposure bracket merging with in-camera rendering recovery"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "lib.rs"

[dependencies]
